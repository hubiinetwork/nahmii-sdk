//settlement details object, which will be added in contract whenever there is a settlement staged
class SettlementHistory {
    constructor(settlement) {}
    get isSettled(testWalletAddress) {
        //check address in settlement object loaded from contract
    }

    from (nonce) {
        //load settlement history from contract using receipt global nonce
    }
}